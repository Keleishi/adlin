server {
    listen 80;
    server_name {{ item.name }}.{{ domain_suffix }};
    root {{ web_root }}/{{ item.name }}.{{ domain_suffix }};
    index index.html;

    location / {
        return 200 'OK from {{ item.name }}';
        add_header Content-Type text/plain;
    }
}
